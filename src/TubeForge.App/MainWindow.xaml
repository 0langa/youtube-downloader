<Window x:Class="TubeForge.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TubeForge" Icon="Assets/TubeForge.ico" Height="820" Width="1240"
        MinHeight="680" MinWidth="980"
        UseLayoutRounding="True" SnapsToDevicePixels="True"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Geometry x:Key="DownloadNavIcon">M2,10 L8,16 L14,10 M8,2 L8,16 M2,20 L14,20</Geometry>
        <Geometry x:Key="QueueNavIcon">M3,4 L15,4 M3,10 L15,10 M3,16 L15,16</Geometry>
        <Geometry x:Key="LibraryNavIcon">M2,3 L16,3 L16,17 L2,17 Z M5,7 L13,7 M5,11 L13,11</Geometry>
        <Geometry x:Key="SettingsNavIcon">M9,2 A7,7 0 1 1 8.99,2 M9,6 A3,3 0 1 1 8.99,6</Geometry>
        <Geometry x:Key="DiagnosticsNavIcon">M9,2 L16,9 L9,16 L2,9 Z M9,6 L9,10 M9,13 L9,13.1</Geometry>
    </Window.Resources>
    <Grid Background="{StaticResource WindowBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="226" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{StaticResource SidebarBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0"
                IsEnabled="{Binding IsMainContentEnabled}">
            <Grid Margin="20,24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="32" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal">
                    <Border Width="36" Height="36" CornerRadius="10" Background="{StaticResource AccentBrush}">
                        <Viewbox Margin="6">
                            <Grid Width="32" Height="32">
                                <Path Fill="White" Data="M 8,5.9 C 7.1,5.5 6.3,6 6.3,6.8 L 6.3,25.2 C 6.3,26 7.2,26.5 7.9,26.1 L 24,16.8 C 24.7,16.4 24.7,15.5 24,15.1 Z" />
                                <Path Fill="#FFAD42" Data="M 25,2 L 26,5 L 29,6 L 26,7 L 25,10 L 24,7 L 21,6 L 24,5 Z" />
                            </Grid>
                        </Viewbox>
                    </Border>
                    <StackPanel Margin="11,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="TubeForge" FontSize="18" FontWeight="Bold" />
                        <TextBlock Text="LOCAL MEDIA TOOL" FontSize="9" FontWeight="SemiBold"
                                   Foreground="{StaticResource MutedTextBrush}" />
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Row="2">
                    <Button Command="{Binding ShowDownloadCommand}"
                            AutomationProperties.Name="Download page">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource NavButton}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsDownloadPage}" Value="True">
                                        <Setter Property="Background" Value="#1A2240" />
                                        <Setter Property="Foreground" Value="White" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">
                            <Viewbox Width="18" Height="18"><Path Data="{StaticResource DownloadNavIcon}" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}" StrokeThickness="1.7" StrokeStartLineCap="Round" StrokeEndLineCap="Round" /></Viewbox>
                            <TextBlock Text="Download" Margin="12,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding ShowQueueCommand}"
                            AutomationProperties.Name="Download queue page">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource NavButton}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsQueuePage}" Value="True">
                                        <Setter Property="Background" Value="#1A2240" />
                                        <Setter Property="Foreground" Value="White" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">
                            <Viewbox Width="18" Height="18"><Path Data="{StaticResource QueueNavIcon}" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}" StrokeThickness="1.7" StrokeStartLineCap="Round" StrokeEndLineCap="Round" /></Viewbox>
                            <TextBlock Text="Queue" Margin="12,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding ShowLibraryCommand}"
                            AutomationProperties.Name="Download library page">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource NavButton}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsLibraryPage}" Value="True">
                                        <Setter Property="Background" Value="#1A2240" />
                                        <Setter Property="Foreground" Value="White" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">
                            <Viewbox Width="18" Height="18"><Path Data="{StaticResource LibraryNavIcon}" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}" StrokeThickness="1.7" StrokeStartLineCap="Round" StrokeEndLineCap="Round" /></Viewbox>
                            <TextBlock Text="Library" Margin="12,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding ShowSettingsCommand}"
                            AutomationProperties.Name="Settings page">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource NavButton}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsSettingsPage}" Value="True">
                                        <Setter Property="Background" Value="#1A2240" />
                                        <Setter Property="Foreground" Value="White" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">
                            <Viewbox Width="18" Height="18"><Path Data="{StaticResource SettingsNavIcon}" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}" StrokeThickness="1.7" /></Viewbox>
                            <TextBlock Text="Settings" Margin="12,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding ShowDiagnosticsCommand}"
                            AutomationProperties.Name="Diagnostics page">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource NavButton}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsDiagnosticsPage}" Value="True">
                                        <Setter Property="Background" Value="#1A2240" />
                                        <Setter Property="Foreground" Value="White" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">
                            <Viewbox Width="18" Height="18"><Path Data="{StaticResource DiagnosticsNavIcon}" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}" StrokeThickness="1.7" StrokeStartLineCap="Round" StrokeEndLineCap="Round" /></Viewbox>
                            <TextBlock Text="Diagnostics" Margin="12,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </StackPanel>

                <Border Grid.Row="4" Background="#111A2B" BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="1" CornerRadius="10" Padding="12">
                    <StackPanel>
                        <TextBlock Text="RESPONSIBLE USE" FontSize="10" FontWeight="Bold"
                                   Foreground="{StaticResource SuccessBrush}" />
                        <TextBlock Text="Save only media you own or have permission to download. No DRM or access-control bypass."
                                   Margin="0,7,0,0" TextWrapping="Wrap" FontSize="11" LineHeight="16"
                                   Foreground="{StaticResource MutedTextBrush}" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                      IsEnabled="{Binding IsMainContentEnabled}"
                      Visibility="{Binding IsDownloadPage, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid Margin="38,30,38,40" MaxWidth="1020">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="24" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="14" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="14" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="14" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <StackPanel>
                        <TextBlock Text="Download media" FontSize="29" FontWeight="SemiBold" />
                        <TextBlock Text="Analyze a YouTube URL, choose the exact stream, save locally."
                                   Margin="0,7,0,0" FontSize="14" Foreground="{StaticResource MutedTextBrush}" />
                    </StackPanel>
                    <Border HorizontalAlignment="Right" VerticalAlignment="Top" Background="#10251F"
                            BorderBrush="#245E4B" BorderThickness="1" CornerRadius="12" Padding="10,6">
                        <TextBlock Text="●  COMPATIBLE MP4" FontSize="10" FontWeight="Bold"
                                   Foreground="{StaticResource SuccessBrush}" />
                    </Border>
                </Grid>

                <Border Grid.Row="2" Style="{StaticResource Card}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="12" />
                            <ColumnDefinition Width="130" />
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="UrlTextBox" Grid.Column="0" Text="{Binding UrlText, UpdateSourceTrigger=PropertyChanged}"
                                 ToolTip="YouTube video, playlist, or channel URL"
                                 AutomationProperties.Name="YouTube video, playlist, or channel URL"
                                 KeyDown="UrlTextBox_OnKeyDown" />
                        <Button Grid.Column="2" Style="{StaticResource PrimaryButton}" Content="Analyze"
                                Command="{Binding AnalyzeCommand}" AutomationProperties.Name="Analyze YouTube URL" />
                    </Grid>
                </Border>

                <Border Grid.Row="4" Style="{StaticResource Card}" BorderBrush="#613143"
                        AutomationProperties.LiveSetting="Assertive"
                        Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="!" Foreground="{StaticResource ErrorBrush}" FontSize="18" FontWeight="Bold" />
                            <TextBlock Text="{Binding ErrorMessage}" Margin="12,1,0,0" TextWrapping="Wrap"
                                       Foreground="#FFC4CE" />
                        </StackPanel>
                        <WrapPanel Margin="30,12,0,0">
                            <Button Style="{StaticResource SecondaryButton}" Content="Change destination"
                                    Click="BrowseButton_OnClick" Padding="11,7" />
                            <Button Style="{StaticResource SecondaryButton}" Content="Open settings"
                                    Command="{Binding ShowSettingsCommand}" Padding="11,7" Margin="8,0,0,0" />
                            <Button Style="{StaticResource SecondaryButton}" Content="Open diagnostics"
                                    Command="{Binding ShowDiagnosticsCommand}" Padding="11,7" Margin="8,0,0,0" />
                            <Button Style="{StaticResource SecondaryButton}" Content="Copy report"
                                    Click="CopyDiagnosticsButton_OnClick" Padding="11,7" Margin="8,0,0,0" />
                        </WrapPanel>
                    </StackPanel>
                </Border>

                <Grid Grid.Row="6" Visibility="{Binding HasVideo, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="320" />
                        <ColumnDefinition Width="14" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <Border Style="{StaticResource Card}" Padding="0" ClipToBounds="True">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="180" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Border Background="#080C14">
                                    <Image Source="{Binding ThumbnailUrl}" Stretch="UniformToFill" />
                                </Border>
                                <StackPanel Grid.Row="1" Margin="18">
                                    <TextBlock Text="{Binding VideoTitle}" FontSize="16" FontWeight="SemiBold"
                                               TextWrapping="Wrap" MaxHeight="52" />
                                    <TextBlock Text="{Binding VideoMetaLine}" Margin="0,8,0,0" FontSize="12"
                                               Foreground="{StaticResource MutedTextBrush}" TextTrimming="CharacterEllipsis" />
                                    <Border Background="#1B2335" CornerRadius="6" Padding="8,5" Margin="0,13,0,0"
                                            HorizontalAlignment="Left">
                                        <TextBlock Text="{Binding ExtractionStatus}" FontSize="10"
                                                   Foreground="{StaticResource SuccessBrush}" />
                                    </Border>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <Border Style="{StaticResource Card}" Margin="0,14,0,0">
                            <StackPanel>
                                <TextBlock Text="Destination" FontSize="13" FontWeight="SemiBold" />
                                <Grid Margin="0,9,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="8" />
                                        <ColumnDefinition Width="74" />
                                    </Grid.ColumnDefinitions>
                                    <TextBox Grid.Column="0" Text="{Binding DownloadFolder, UpdateSourceTrigger=PropertyChanged}"
                                             FontSize="12" Padding="10,8" AutomationProperties.Name="Download folder" />
                                    <Button Grid.Column="2" Style="{StaticResource SecondaryButton}" Content="Browse"
                                            Padding="9,8" Click="BrowseButton_OnClick" />
                                </Grid>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource Card}" Margin="0,14,0,0"
                                Visibility="{Binding HasCaptions, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <StackPanel>
                                <TextBlock Text="Captions" FontSize="13" FontWeight="SemiBold" />
                                <TextBlock Text="Save a manual or auto-generated language track beside the media."
                                           Margin="0,5,0,0" FontSize="10" TextWrapping="Wrap"
                                           Foreground="{StaticResource MutedTextBrush}" />
                                <TextBlock Text="LANGUAGE TRACK" Style="{StaticResource FieldLabel}" Margin="0,14,0,5" />
                                <ComboBox ItemsSource="{Binding CaptionTracks}" SelectedItem="{Binding SelectedCaptionTrack}"
                                          AutomationProperties.Name="Caption language and track type" />
                                <TextBlock Text="CAPTION FORMAT" Style="{StaticResource FieldLabel}" Margin="0,12,0,5" />
                                <ComboBox ItemsSource="{Binding CaptionFormats}" SelectedItem="{Binding SelectedCaptionFormat}"
                                          AutomationProperties.Name="Caption output format" />
                                <TextBlock Text="EMBED TRACKS IN NEXT MEDIA DOWNLOAD" Style="{StaticResource FieldLabel}" Margin="0,12,0,5" />
                                <ItemsControl ItemsSource="{Binding CaptionTracks}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <CheckBox Content="{Binding Label}"
                                                      IsChecked="{Binding IsSelectedForEmbedding}"
                                                      IsEnabled="{Binding DataContext.CanEmbedSelectedCaption, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                                                      Margin="0,3,0,0"
                                                      AutomationProperties.Name="{Binding Label, StringFormat=Embed caption track {0}}" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <TextBlock Text="Choose up to eight soft-subtitle tracks. Single-video Audio + video outputs only. Media stays stream-copy unless its selected output preset requires conversion."
                                           Margin="24,4,0,0" FontSize="10" TextWrapping="Wrap"
                                           Foreground="{StaticResource MutedTextBrush}" />
                                <Button Style="{StaticResource SecondaryButton}" Content="Save caption" Margin="0,12,0,0"
                                        Command="{Binding DownloadCaptionCommand}"
                                        AutomationProperties.Name="Save selected caption" />
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource Card}" Margin="0,14,0,0"
                                Visibility="{Binding HasChapters, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <StackPanel>
                                <TextBlock Text="Chapters" FontSize="13" FontWeight="SemiBold" />
                                <TextBlock Text="Keep the video's named timeline markers in the downloaded media."
                                           Margin="0,5,0,0" FontSize="10" TextWrapping="Wrap"
                                           Foreground="{StaticResource MutedTextBrush}" />
                                <CheckBox Content="Embed chapters in next media download"
                                          IsChecked="{Binding EmbedChapters}"
                                          IsEnabled="{Binding CanEmbedChapters}"
                                          Margin="0,12,0,0"
                                          AutomationProperties.Name="Embed video chapters in media" />
                                <TextBlock Text="Single-video outputs only. MP4, MKV, and WebM are finalized atomically with bundled FFmpeg."
                                           Margin="24,4,0,0" FontSize="10" TextWrapping="Wrap"
                                           Foreground="{StaticResource MutedTextBrush}" />
                                <CheckBox Content="Create separate chapter files after download"
                                          IsChecked="{Binding SplitChapters}"
                                          IsEnabled="{Binding CanSplitChapters}"
                                          Margin="0,12,0,0"
                                          AutomationProperties.Name="Create separate media files for video chapters" />
                                <TextBlock Text="Keeps the full media file and atomically adds a sibling chapters folder using lossless stream copy; starts align to decodable keyframes."
                                           Margin="24,4,0,0" FontSize="10" TextWrapping="Wrap"
                                           Foreground="{StaticResource MutedTextBrush}" />
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource Card}" Margin="0,14,0,0"
                                Visibility="{Binding IsLiveCapture, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <StackPanel>
                                <TextBlock Text="Public live capture" FontSize="13" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding LiveCaptureModeNotice}"
                                           Margin="0,5,0,0" FontSize="10" TextWrapping="Wrap"
                                           Foreground="{StaticResource MutedTextBrush}" />
                                <Grid Margin="0,12,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="10" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="10" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="CAPTURE MINUTES" Style="{StaticResource FieldLabel}" />
                                        <TextBox Text="{Binding LiveDurationMinutesText, UpdateSourceTrigger=PropertyChanged}"
                                                 Margin="0,6,0,0"
                                                 AutomationProperties.Name="Maximum live capture minutes" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="MAXIMUM GIB" Style="{StaticResource FieldLabel}" />
                                        <TextBox Text="{Binding LiveMaximumSizeGiBText, UpdateSourceTrigger=PropertyChanged}"
                                                 Margin="0,6,0,0"
                                                 AutomationProperties.Name="Maximum live capture size in GiB" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="4">
                                        <TextBlock Text="WAIT MINUTES" Style="{StaticResource FieldLabel}" />
                                        <TextBox Text="{Binding LiveMaximumWaitMinutesText, UpdateSourceTrigger=PropertyChanged}"
                                                 Margin="0,6,0,0"
                                                 IsEnabled="{Binding IsUpcomingLiveCapture}"
                                                 AutomationProperties.Name="Maximum minutes to wait for upcoming live stream" />
                                    </StackPanel>
                                </Grid>
                                <TextBlock Text="Public, unencrypted HLS only. Output is recoverable MKV stream copy. Pause preserves the local segment journal; missing, expired, encrypted, DRM, or login-required streams fail closed."
                                           Margin="0,9,0,0" FontSize="10" TextWrapping="Wrap"
                                           Foreground="{StaticResource MutedTextBrush}" />
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource Card}" Margin="0,14,0,0"
                                Visibility="{Binding HasVideo, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <StackPanel>
                                <TextBlock Text="SponsorBlock" FontSize="13" FontWeight="SemiBold" />
                                <TextBlock Text="Optional third-party community timeline data. No request is made until you enable this for a download."
                                           Margin="0,5,0,0" FontSize="10" TextWrapping="Wrap"
                                           Foreground="{StaticResource MutedTextBrush}" />
                                <CheckBox Content="Use SponsorBlock for next media download"
                                          IsChecked="{Binding EnableSponsorBlock}"
                                          IsEnabled="{Binding CanUseSponsorBlock}"
                                          Margin="0,12,0,0"
                                          AutomationProperties.Name="Use SponsorBlock for next media download" />
                                <TextBlock Text="MODE" Style="{StaticResource FieldLabel}" Margin="0,12,0,5" />
                                <ComboBox ItemsSource="{Binding SponsorBlockModeOptions}"
                                          SelectedItem="{Binding SelectedSponsorBlockMode}"
                                          IsEnabled="{Binding EnableSponsorBlock}"
                                          AutomationProperties.Name="SponsorBlock processing mode" />
                                <WrapPanel Margin="0,10,0,0" IsEnabled="{Binding EnableSponsorBlock}">
                                    <CheckBox Content="Sponsor" IsChecked="{Binding SponsorCategory}" Margin="0,0,14,6" />
                                    <CheckBox Content="Intro" IsChecked="{Binding IntroCategory}" Margin="0,0,14,6" />
                                    <CheckBox Content="Outro" IsChecked="{Binding OutroCategory}" Margin="0,0,14,6" />
                                    <CheckBox Content="Self-promo" IsChecked="{Binding SelfPromotionCategory}" Margin="0,0,14,6" />
                                    <CheckBox Content="Interaction" IsChecked="{Binding InteractionCategory}" Margin="0,0,14,6" />
                                    <CheckBox Content="Preview" IsChecked="{Binding PreviewCategory}" Margin="0,0,14,6" />
                                    <CheckBox Content="Filler" IsChecked="{Binding FillerCategory}" Margin="0,0,14,6" />
                                </WrapPanel>
                                <TextBlock Text="{Binding SponsorBlockModeNotice}"
                                           Margin="0,4,0,0" FontSize="10" TextWrapping="Wrap"
                                           Foreground="{StaticResource MutedTextBrush}" />
                                <TextBlock Text="Privacy: TubeForge sends SponsorBlock a four-character SHA-256 prefix, not the full video ID. The response can include multiple candidates and is matched locally; payloads and IDs are never logged."
                                           Margin="0,8,0,0" FontSize="10" TextWrapping="Wrap"
                                           Foreground="{StaticResource MutedTextBrush}" />
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource Card}" Margin="0,14,0,0"
                                Visibility="{Binding HasVideo, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <StackPanel>
                                <TextBlock Text="Trim" FontSize="13" FontWeight="SemiBold" />
                                <TextBlock Text="Keep only a selected part of the next single-video download."
                                           Margin="0,5,0,0" FontSize="10" TextWrapping="Wrap"
                                           Foreground="{StaticResource MutedTextBrush}" />
                                <CheckBox Content="Trim next media download"
                                          IsChecked="{Binding EnableTrim}"
                                          IsEnabled="{Binding CanTrim}"
                                          Margin="0,12,0,0"
                                          AutomationProperties.Name="Trim next media download" />
                                <Grid Margin="0,12,0,0" IsEnabled="{Binding EnableTrim}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="12" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="START (HH:MM:SS)" Style="{StaticResource FieldLabel}" />
                                        <TextBox Text="{Binding TrimStartText, UpdateSourceTrigger=PropertyChanged}"
                                                 Margin="0,6,0,0"
                                                 AutomationProperties.Name="Trim start time" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="END (HH:MM:SS)" Style="{StaticResource FieldLabel}" />
                                        <TextBox Text="{Binding TrimEndText, UpdateSourceTrigger=PropertyChanged}"
                                                 Margin="0,6,0,0"
                                                 AutomationProperties.Name="Trim end time" />
                                    </StackPanel>
                                </Grid>
                                <TextBlock Text="Original-quality outputs use lossless stream copy and may align the start to a nearby keyframe. A selected conversion preset re-encodes the chosen range for a precise cut."
                                           Margin="0,8,0,0" FontSize="10" TextWrapping="Wrap"
                                           Foreground="{StaticResource MutedTextBrush}" />
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource Card}" Margin="0,14,0,0"
                                Visibility="{Binding HasVideo, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <StackPanel>
                                <TextBlock Text="Sidecars" FontSize="13" FontWeight="SemiBold" />
                                <TextBlock Text="Save the highest thumbnail or a stable JSON metadata record beside the media."
                                           Margin="0,5,0,0" FontSize="10" TextWrapping="Wrap"
                                           Foreground="{StaticResource MutedTextBrush}" />
                                <Button Style="{StaticResource SecondaryButton}" Content="Save thumbnail" Margin="0,12,0,0"
                                        Command="{Binding SaveThumbnailCommand}"
                                        AutomationProperties.Name="Save highest quality thumbnail" />
                                <Button Style="{StaticResource SecondaryButton}" Content="Save metadata (.json)" Margin="0,8,0,0"
                                        Command="{Binding SaveMetadataCommand}"
                                        AutomationProperties.Name="Save metadata JSON sidecar" />
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <Border Grid.Column="2" Style="{StaticResource Card}">
                        <StackPanel>
                            <Grid>
                                <StackPanel>
                                    <TextBlock Text="Build your download" FontSize="17" FontWeight="SemiBold" />
                                    <TextBlock Text="Start with a practical preset, then refine any detail."
                                               Margin="0,4,0,0" FontSize="11" Foreground="{StaticResource MutedTextBrush}" />
                                </StackPanel>
                                <TextBlock Text="{Binding FormatCountLabel}" HorizontalAlignment="Right" VerticalAlignment="Center"
                                           Foreground="{StaticResource MutedTextBrush}" FontSize="11" />
                            </Grid>

                            <TextBlock Text="QUICK PRESET" Style="{StaticResource FieldLabel}" Margin="0,18,0,6" />
                            <ComboBox ItemsSource="{Binding DownloadPresets}"
                                      SelectedItem="{Binding SelectedDownloadPreset}"
                                      AutomationProperties.Name="Quick download preset" MinHeight="48">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Label}" FontWeight="SemiBold" FontSize="13" />
                                            <TextBlock Text="{Binding Description}" Margin="0,3,0,0" FontSize="10"
                                                       Foreground="{StaticResource MutedTextBrush}" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>

                            <CheckBox Content="Show advanced format controls"
                                      IsChecked="{Binding ShowAdvancedDownloadOptions}"
                                      Margin="0,14,0,0"
                                      AutomationProperties.Name="Show advanced download format controls" />

                            <StackPanel Visibility="{Binding ShowAdvancedDownloadOptions, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <TextBlock Text="DOWNLOAD TYPE" Style="{StaticResource FieldLabel}" Margin="0,16,0,6" />
                            <ComboBox ItemsSource="{Binding DownloadModes}" SelectedItem="{Binding SelectedDownloadMode}"
                                      AutomationProperties.Name="Download type">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Label}" FontWeight="SemiBold" FontSize="13" />
                                            <TextBlock Text="{Binding Description}" Margin="0,3,0,0" FontSize="10"
                                                       Foreground="{StaticResource MutedTextBrush}" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>

                            <Border Background="#0E1A2B" BorderBrush="#29405F" BorderThickness="1"
                                    CornerRadius="8" Padding="11,9" Margin="0,10,0,0">
                                <TextBlock Text="{Binding ModeNotice}" TextWrapping="Wrap" FontSize="10"
                                           LineHeight="15" Foreground="#A8B7CE" />
                            </Border>

                            <Grid Margin="0,16,0,0"
                                  Visibility="{Binding HasVideoFilters, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="12" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="12" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="12" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Grid.Column="0">
                                    <TextBlock Text="QUALITY" Style="{StaticResource FieldLabel}" />
                                    <ComboBox ItemsSource="{Binding ResolutionOptions}" SelectedItem="{Binding SelectedResolution}"
                                              DisplayMemberPath="Label" AutomationProperties.Name="Video quality" />
                                </StackPanel>
                                <StackPanel Grid.Row="0" Grid.Column="2">
                                    <TextBlock Text="CONTAINER" Style="{StaticResource FieldLabel}" />
                                    <ComboBox ItemsSource="{Binding ContainerOptions}" SelectedItem="{Binding SelectedContainer}"
                                              DisplayMemberPath="Label" AutomationProperties.Name="Video container" />
                                </StackPanel>
                                <StackPanel Grid.Row="2" Grid.Column="0">
                                    <TextBlock Text="VIDEO CODEC" Style="{StaticResource FieldLabel}" />
                                    <ComboBox ItemsSource="{Binding VideoCodecOptions}" SelectedItem="{Binding SelectedVideoCodec}"
                                              DisplayMemberPath="Label" AutomationProperties.Name="Video codec" />
                                </StackPanel>
                                <StackPanel Grid.Row="2" Grid.Column="2">
                                    <TextBlock Text="FRAME RATE" Style="{StaticResource FieldLabel}" />
                                    <ComboBox ItemsSource="{Binding FrameRateOptions}" SelectedItem="{Binding SelectedFrameRate}"
                                              DisplayMemberPath="Label" AutomationProperties.Name="Frame rate" />
                                </StackPanel>
                                <StackPanel Grid.Row="4" Grid.Column="0">
                                    <TextBlock Text="DYNAMIC RANGE" Style="{StaticResource FieldLabel}" />
                                    <ComboBox ItemsSource="{Binding DynamicRangeOptions}" SelectedItem="{Binding SelectedDynamicRange}"
                                              DisplayMemberPath="Label" AutomationProperties.Name="Dynamic range" />
                                </StackPanel>
                            </Grid>

                            <Grid Margin="0,16,0,0"
                                  Visibility="{Binding IsAudioVideo, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="12" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="AUDIO QUALITY" Style="{StaticResource FieldLabel}" />
                                    <ComboBox ItemsSource="{Binding BitrateOptions}" SelectedItem="{Binding SelectedBitrate}"
                                              DisplayMemberPath="Label" AutomationProperties.Name="Muxed audio quality" />
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="AUDIO CODEC" Style="{StaticResource FieldLabel}" />
                                    <ComboBox ItemsSource="{Binding AudioCodecOptions}" SelectedItem="{Binding SelectedAudioCodec}"
                                              DisplayMemberPath="Label" AutomationProperties.Name="Muxed audio codec" />
                                </StackPanel>
                            </Grid>

                            <StackPanel Margin="0,16,0,0"
                                        Visibility="{Binding IsAudioVideo, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <TextBlock Text="OUTPUT PRESET" Style="{StaticResource FieldLabel}" />
                                <ComboBox ItemsSource="{Binding VideoProcessingOptions}"
                                          SelectedItem="{Binding SelectedVideoProcessing}"
                                          AutomationProperties.Name="Video output preset" MinHeight="48">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel>
                                                <TextBlock Text="{Binding Label}" />
                                                <TextBlock Text="{Binding Description}" FontSize="10"
                                                           Foreground="{StaticResource MutedTextBrush}" />
                                            </StackPanel>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </StackPanel>

                            <Grid Margin="0,16,0,0"
                                  Visibility="{Binding IsAudioOnly, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="12" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="12" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Grid.Column="0">
                                    <TextBlock Text="BITRATE" Style="{StaticResource FieldLabel}" />
                                    <ComboBox ItemsSource="{Binding BitrateOptions}" SelectedItem="{Binding SelectedBitrate}"
                                              DisplayMemberPath="Label" AutomationProperties.Name="Audio bitrate" />
                                </StackPanel>
                                <StackPanel Grid.Row="0" Grid.Column="2">
                                    <TextBlock Text="FILE FORMAT" Style="{StaticResource FieldLabel}" />
                                    <ComboBox ItemsSource="{Binding ContainerOptions}" SelectedItem="{Binding SelectedContainer}"
                                              DisplayMemberPath="Label" AutomationProperties.Name="Audio file format" />
                                </StackPanel>
                                <StackPanel Grid.Row="2" Grid.Column="0">
                                    <TextBlock Text="AUDIO CODEC" Style="{StaticResource FieldLabel}" />
                                    <ComboBox ItemsSource="{Binding AudioCodecOptions}" SelectedItem="{Binding SelectedAudioCodec}"
                                              DisplayMemberPath="Label" AutomationProperties.Name="Audio codec" />
                                </StackPanel>
                                <StackPanel Grid.Row="2" Grid.Column="2">
                                    <TextBlock Text="PROCESSING" Style="{StaticResource FieldLabel}" />
                                    <ComboBox ItemsSource="{Binding AudioProcessingOptions}"
                                              SelectedItem="{Binding SelectedAudioProcessing}"
                                              AutomationProperties.Name="Audio processing" MinHeight="48">
                                        <ComboBox.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel>
                                                    <TextBlock Text="{Binding Label}" />
                                                    <TextBlock Text="{Binding Description}" FontSize="10"
                                                               Foreground="{StaticResource MutedTextBrush}" />
                                                </StackPanel>
                                            </DataTemplate>
                                        </ComboBox.ItemTemplate>
                                    </ComboBox>
                                </StackPanel>
                            </Grid>

                            <TextBlock Text="EXACT OUTPUT" Style="{StaticResource FieldLabel}" Margin="0,16,0,6" />
                            <ComboBox ItemsSource="{Binding Formats}" SelectedItem="{Binding SelectedFormat}"
                                      AutomationProperties.Name="Exact matching stream" MinHeight="54">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <StackPanel>
                                                <TextBlock Text="{Binding Label}" FontWeight="SemiBold" FontSize="12" />
                                                <TextBlock Text="{Binding TechnicalLabel}" Margin="0,3,0,0" FontSize="10"
                                                           Foreground="{StaticResource MutedTextBrush}" />
                                            </StackPanel>
                                            <TextBlock Grid.Column="1" Text="{Binding SizeLabel}" Margin="14,0,0,0"
                                                       VerticalAlignment="Center" FontSize="10"
                                                       Foreground="{StaticResource MutedTextBrush}" />
                                        </Grid>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                            </StackPanel>

                            <ProgressBar Minimum="0" Maximum="1" Value="{Binding DownloadFraction, Mode=OneWay}"
                                         Margin="0,16,0,0"
                                         AutomationProperties.Name="Latest active download progress"
                                         Visibility="{Binding IsDownloading, Converter={StaticResource BooleanToVisibilityConverter}}" />

                            <Grid Margin="0,16,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="12" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="8" />
                                    <ColumnDefinition Width="150" />
                                </Grid.ColumnDefinitions>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="{Binding StatusMessage}" FontSize="11" TextTrimming="CharacterEllipsis"
                                               AutomationProperties.LiveSetting="Polite"
                                               Foreground="{StaticResource MutedTextBrush}" />
                                    <TextBlock Text="{Binding ProgressDetail}" Margin="0,3,0,0" FontSize="10"
                                               Foreground="{StaticResource MutedTextBrush}" />
                                </StackPanel>
                                <Button Grid.Column="2" Style="{StaticResource SecondaryButton}" Content="Pause all"
                                        Command="{Binding CancelCommand}" Padding="14,10"
                                        Visibility="{Binding IsDownloading, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                <Button Grid.Column="4" Style="{StaticResource PrimaryButton}" Content="{Binding DownloadActionLabel}"
                                        Command="{Binding DownloadCommand}" AutomationProperties.Name="Start download" />
                            </Grid>
                        </StackPanel>
                    </Border>
                </Grid>

                <Border Grid.Row="6" Style="{StaticResource Card}"
                        Visibility="{Binding HasCollection, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="16" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="14" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="14" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="14" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0">
                            <StackPanel>
                                <TextBlock Text="{Binding CollectionTitle}" FontSize="20" FontWeight="SemiBold"
                                           TextWrapping="Wrap" />
                                <TextBlock Text="{Binding CollectionSummary}" Margin="0,5,0,0" FontSize="11"
                                           Foreground="{StaticResource MutedTextBrush}" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                                <Button Style="{StaticResource SecondaryButton}" Content="Select all" Padding="13,8"
                                        Command="{Binding SelectAllCollectionCommand}" />
                                <Button Style="{StaticResource SecondaryButton}" Content="Select none" Padding="13,8"
                                        Margin="8,0,0,0" Command="{Binding SelectNoneCollectionCommand}" />
                                <Button Style="{StaticResource SecondaryButton}" Content="Select missing" Padding="13,8"
                                        Margin="8,0,0,0" Command="{Binding SelectMissingCollectionCommand}"
                                        ToolTip="Select videos absent from both Queue and Library" />
                            </StackPanel>
                        </Grid>

                        <Border Grid.Row="2" Background="#0E1A2B" BorderBrush="#29405F" BorderThickness="1"
                                CornerRadius="8" Padding="12,10">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="12" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="14" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="10" />
                                    <ColumnDefinition Width="86" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="DESTINATION" Style="{StaticResource FieldLabel}" VerticalAlignment="Center" />
                                <TextBox Grid.Column="2" Text="{Binding DownloadFolder, UpdateSourceTrigger=PropertyChanged}"
                                         AutomationProperties.Name="Collection download folder" />
                                <Button Grid.Column="4" Style="{StaticResource SecondaryButton}" Content="Browse"
                                        Click="BrowseButton_OnClick" />

                                <Grid Grid.Row="2" Grid.ColumnSpan="5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180" />
                                        <ColumnDefinition Width="12" />
                                        <ColumnDefinition Width="220" />
                                        <ColumnDefinition Width="16" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="16" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="ARCHIVE OUTPUT" Style="{StaticResource FieldLabel}" />
                                        <ComboBox ItemsSource="{Binding ArchiveOutputPresets}"
                                                  SelectedItem="{Binding SelectedArchiveOutputPreset}"
                                                  Margin="0,5,0,0" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="CAPTIONS" Style="{StaticResource FieldLabel}" />
                                        <ComboBox ItemsSource="{Binding ArchiveCaptionPreferences}"
                                                  SelectedItem="{Binding SelectedArchiveCaptionPreference}"
                                                  Margin="0,5,0,0" />
                                    </StackPanel>
                                    <CheckBox Grid.Column="4" IsChecked="{Binding ArchiveEmbedChapters}"
                                              Content="Embed chapters when available" VerticalAlignment="Bottom"
                                              Margin="0,0,0,8" />
                                    <Button Grid.Column="6" Style="{StaticResource SecondaryButton}"
                                            Content="Save archive profile" Padding="13,9" VerticalAlignment="Bottom"
                                            Command="{Binding SaveArchiveProfileCommand}" />
                                </Grid>
                            </Grid>
                        </Border>

                        <ListBox Grid.Row="4" ItemsSource="{Binding CollectionItems}" Height="520"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                                 AutomationProperties.Name="Collection videos">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="3,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="42" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="16" />
                                            <ColumnDefinition Width="220" />
                                        </Grid.ColumnDefinitions>
                                        <CheckBox IsChecked="{Binding IsSelected}" VerticalAlignment="Center"
                                                  AutomationProperties.Name="Select collection video" />
                                        <TextBlock Grid.Column="1" Text="{Binding IndexLabel}" HorizontalAlignment="Center"
                                                   VerticalAlignment="Center" FontWeight="SemiBold"
                                                   Foreground="{StaticResource AccentBrush}" />
                                        <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Title}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                            <TextBlock Text="{Binding Detail}" Margin="0,4,0,0" FontSize="10"
                                                       Foreground="{StaticResource MutedTextBrush}" />
                                        </StackPanel>
                                        <TextBlock Grid.Column="4" Text="{Binding Status}" VerticalAlignment="Center"
                                                   TextAlignment="Right" TextWrapping="Wrap" FontSize="10"
                                                   Foreground="{StaticResource MutedTextBrush}" />
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <Border Grid.Row="6" Background="#111A2A" CornerRadius="8" Padding="12,9">
                            <TextBlock Text="Archive output uses the selected preset and current filename template. Saving a profile marks the currently listed videos as checked; use Select missing, then Add selected to queue for existing gaps. Use {index} in the filename template to include collection positions."
                                       TextWrapping="Wrap" FontSize="10" Foreground="#A8B7CE" />
                        </Border>

                        <Grid Grid.Row="8">
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="{Binding CollectionSelectionSummary}" FontSize="11"
                                           Foreground="{StaticResource MutedTextBrush}" />
                                <TextBlock Text="{Binding ProgressDetail}" Margin="0,3,0,0" FontSize="10"
                                           Foreground="{StaticResource MutedTextBrush}" />
                            </StackPanel>
                            <Button Style="{StaticResource PrimaryButton}" Content="Add selected to queue"
                                    HorizontalAlignment="Right" Width="190"
                                    Command="{Binding QueueCollectionCommand}"
                                    AutomationProperties.Name="Add selected collection videos to queue" />
                        </Grid>
                    </Grid>
                </Border>

                <Border Grid.Row="8" Style="{StaticResource Card}"
                        Visibility="{Binding IsAnalyzing, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <ProgressBar Width="100" IsIndeterminate="True" VerticalAlignment="Center"
                                     AutomationProperties.Name="Analyzing media" />
                        <TextBlock Text="Analyzing YouTube delivery options…" Margin="16,0,0,0"
                                   Grid.Column="1" VerticalAlignment="Center" Foreground="{StaticResource MutedTextBrush}"
                                   AutomationProperties.LiveSetting="Polite" />
                        <Button Grid.Column="2" Style="{StaticResource SecondaryButton}" Content="Cancel analysis"
                                Command="{Binding CancelAnalysisCommand}" Padding="14,9"
                                AutomationProperties.Name="Cancel video analysis" />
                    </Grid>
                </Border>
            </Grid>
        </ScrollViewer>

        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                      IsEnabled="{Binding IsMainContentEnabled}"
                      Visibility="{Binding IsLibraryPage, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid Margin="38,30,38,40" MaxWidth="1020">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="24" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="14" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="14" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="14" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <StackPanel>
                        <TextBlock Text="Library" FontSize="29" FontWeight="SemiBold" />
                        <TextBlock Text="Durable local history powers duplicate detection. No media URLs are stored."
                                   Margin="0,7,0,0" FontSize="14" Foreground="{StaticResource MutedTextBrush}" />
                    </StackPanel>
                    <StackPanel HorizontalAlignment="Right" VerticalAlignment="Bottom">
                        <TextBlock Text="{Binding HistorySummary}" HorizontalAlignment="Right"
                                   Foreground="{StaticResource MutedTextBrush}" FontSize="11" />
                        <Button Style="{StaticResource SecondaryButton}" Content="Clear history" Margin="0,9,0,0"
                                Command="{Binding ClearHistoryCommand}" Padding="13,8" />
                    </StackPanel>
                </Grid>

                <Border Grid.Row="2" Style="{StaticResource Card}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="10" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="12" />
                                <ColumnDefinition Width="190" />
                                <ColumnDefinition Width="12" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="SEARCH LIBRARY" Style="{StaticResource FieldLabel}" />
                                <TextBox Text="{Binding LibrarySearchText, UpdateSourceTrigger=PropertyChanged}"
                                         Margin="0,5,0,0" AutomationProperties.Name="Search completed downloads" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="SORT" Style="{StaticResource FieldLabel}" />
                                <ComboBox ItemsSource="{Binding LibrarySortOptions}"
                                          SelectedItem="{Binding SelectedLibrarySort}"
                                          DisplayMemberPath="Label" Margin="0,5,0,0"
                                          AutomationProperties.Name="Sort completed downloads" />
                            </StackPanel>
                            <Button Grid.Column="4" Style="{StaticResource SecondaryButton}"
                                    Content="Remove missing" Command="{Binding RemoveMissingHistoryCommand}"
                                    Padding="13,9" VerticalAlignment="Bottom" />
                        </Grid>
                        <Grid Grid.Row="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="16" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="HISTORY STORAGE" Style="{StaticResource FieldLabel}" />
                                <TextBlock Text="{Binding HistoryStoragePath}" Margin="0,6,0,0" FontSize="10"
                                           Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" />
                                <TextBlock Text="{Binding LibraryStatus}" Margin="0,7,0,0" FontSize="10"
                                           Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap"
                                           AutomationProperties.LiveSetting="Polite" />
                                <TextBlock Text="Exports contain Library metadata and local file paths, but no media URLs or credentials. Imports merge without deleting current records."
                                           Margin="0,5,0,0" FontSize="9" Foreground="{StaticResource MutedTextBrush}"
                                           TextWrapping="Wrap" />
                            </StackPanel>
                            <WrapPanel Grid.Column="2" VerticalAlignment="Bottom">
                                <Button Style="{StaticResource SecondaryButton}" Content="Export" Padding="13,9"
                                        Click="ExportLibraryButton_OnClick" />
                                <Button Style="{StaticResource SecondaryButton}" Content="Import" Padding="13,9"
                                        Margin="8,0,0,0" Click="ImportLibraryButton_OnClick" />
                                <Button Style="{StaticResource SecondaryButton}" Content="Rescan folder" Padding="13,9"
                                        Margin="8,0,0,0" Click="RescanLibraryButton_OnClick" />
                            </WrapPanel>
                        </Grid>
                    </Grid>
                </Border>

                <Border Grid.Row="4" Style="{StaticResource Card}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="280" />
                            <ColumnDefinition Width="10" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="8" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="PLAYLIST AND CHANNEL ARCHIVES" FontSize="15" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding ArchiveStatus}" Margin="0,6,0,0" FontSize="10"
                                       Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap"
                                       AutomationProperties.LiveSetting="Polite" />
                            <TextBlock Text="{Binding ArchiveStoragePath}" Margin="0,4,0,0" FontSize="9"
                                       Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" />
                        </StackPanel>
                        <ComboBox Grid.Column="2" ItemsSource="{Binding ArchiveProfiles}"
                                  SelectedItem="{Binding SelectedArchiveProfile}"
                                  VerticalAlignment="Center"
                                  AutomationProperties.Name="Saved collection archive profile">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding DisplayName}" TextTrimming="CharacterEllipsis" />
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                        <Button Grid.Column="4" Style="{StaticResource PrimaryButton}" Content="Check all new"
                                Padding="13,9" VerticalAlignment="Center"
                                Command="{Binding CheckArchiveProfilesCommand}" />
                        <Button Grid.Column="6" Style="{StaticResource SecondaryButton}" Content="Remove profile"
                                Padding="13,9" VerticalAlignment="Center"
                                Command="{Binding RemoveArchiveProfileCommand}" />
                    </Grid>
                </Border>

                <Border Grid.Row="6">
                    <Border.Style>
                        <Style TargetType="Border" BasedOn="{StaticResource Card}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasHistory}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel HorizontalAlignment="Center" Margin="20">
                        <TextBlock Text="Library empty" FontSize="17" FontWeight="SemiBold" HorizontalAlignment="Center" />
                        <TextBlock Text="{Binding HistoryEmptyMessage}"
                                   Margin="0,7,0,0" Foreground="{StaticResource MutedTextBrush}" />
                    </StackPanel>
                </Border>

                <ListBox Grid.Row="8" ItemsSource="{Binding HistoryItems}" Height="600"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         VirtualizingPanel.IsVirtualizing="True"
                         VirtualizingPanel.VirtualizationMode="Recycling"
                         AutomationProperties.Name="Completed download history">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource Card}" Margin="0,0,0,12">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="12" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="8" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{Binding Title}" FontSize="15" FontWeight="SemiBold"
                                                   TextTrimming="CharacterEllipsis" />
                                        <TextBlock Text="{Binding Detail}" Margin="0,5,0,0" FontSize="10"
                                                   Foreground="{StaticResource MutedTextBrush}" />
                                        <TextBlock Text="{Binding Destination}" Margin="0,4,0,0" FontSize="9"
                                                   Foreground="{StaticResource MutedTextBrush}" TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                    <Button Grid.Column="2" Style="{StaticResource SecondaryButton}" Content="Open folder"
                                            Command="{Binding RevealCommand}" Padding="12,8" VerticalAlignment="Center" />
                                    <Button Grid.Column="4" Style="{StaticResource SecondaryButton}" Content="Remove record"
                                            Command="{Binding RemoveCommand}" Padding="12,8" VerticalAlignment="Center" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </ScrollViewer>

        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                      IsEnabled="{Binding IsMainContentEnabled}"
                      Visibility="{Binding IsQueuePage, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid Margin="38,30,38,40" MaxWidth="1020">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="24" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="14" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="14" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="14" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <StackPanel>
                        <TextBlock Text="Download queue" FontSize="29" FontWeight="SemiBold" />
                        <TextBlock Text="Rate-aware transfers, recovery, and per-item controls."
                                   Margin="0,7,0,0" FontSize="14" Foreground="{StaticResource MutedTextBrush}" />
                    </StackPanel>
                    <TextBlock Text="{Binding QueueSummary}" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                               Foreground="{StaticResource MutedTextBrush}" FontSize="11" />
                </Grid>

                <Border Grid.Row="2" Style="{StaticResource Card}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="12" />
                            <ColumnDefinition Width="110" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="10" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="GLOBAL CONCURRENCY" Style="{StaticResource FieldLabel}" />
                            <TextBlock Text="Global 1–4 · provider host cap 2 · Retry-After honored" Margin="0,4,0,0" FontSize="10"
                                       Foreground="{StaticResource MutedTextBrush}" />
                        </StackPanel>
                        <ComboBox Grid.Column="2" ItemsSource="{Binding MaxConcurrentDownloadOptions}"
                                  SelectedItem="{Binding SelectedMaxConcurrentDownloads}" VerticalAlignment="Center"
                                  AutomationProperties.Name="Maximum concurrent downloads" />
                        <Button Grid.Column="4" Style="{StaticResource SecondaryButton}" Content="Pause all"
                                Command="{Binding CancelCommand}" Padding="14,10" VerticalAlignment="Center"
                                Visibility="{Binding IsDownloading, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        <Button Grid.Column="6" Style="{StaticResource SecondaryButton}" Content="Clear completed"
                                Command="{Binding ClearCompletedCommand}" Padding="14,10" VerticalAlignment="Center" />
                    </Grid>
                </Border>

                <Border Grid.Row="4" Style="{StaticResource Card}" BorderBrush="#613143"
                        Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="!" Foreground="{StaticResource ErrorBrush}" FontSize="18" FontWeight="Bold" />
                        <TextBlock Text="{Binding ErrorMessage}" Margin="12,1,0,0" TextWrapping="Wrap"
                                   Foreground="#FFC4CE" />
                    </StackPanel>
                </Border>

                <Border Grid.Row="6">
                    <Border.Style>
                        <Style TargetType="Border" BasedOn="{StaticResource Card}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasQueueItems}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel HorizontalAlignment="Center" Margin="20">
                        <TextBlock Text="Queue empty" FontSize="17" FontWeight="SemiBold" HorizontalAlignment="Center" />
                        <TextBlock Text="Analyze media, choose output, then use Add to queue."
                                   Margin="0,7,0,0" Foreground="{StaticResource MutedTextBrush}" />
                        <Button Style="{StaticResource PrimaryButton}" Content="Add a download" Margin="0,18,0,0"
                                Command="{Binding ShowDownloadCommand}" HorizontalAlignment="Center" />
                    </StackPanel>
                </Border>

                <ItemsControl Grid.Row="8" ItemsSource="{Binding QueueItems}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource Card}" Margin="0,0,0,12">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Title}" FontSize="15" FontWeight="SemiBold"
                                                       TextTrimming="CharacterEllipsis" />
                                            <TextBlock Text="{Binding FileName}" Margin="0,5,0,0" FontSize="11"
                                                       Foreground="{StaticResource MutedTextBrush}" TextTrimming="CharacterEllipsis" />
                                        </StackPanel>
                                        <Border Grid.Column="1" Background="{Binding StatusBackground}" BorderBrush="{Binding StatusBorder}"
                                                BorderThickness="1" CornerRadius="8" Padding="9,5" VerticalAlignment="Top">
                                            <TextBlock Text="{Binding StatusLabel}" FontSize="9" FontWeight="Bold"
                                                       Foreground="{Binding StatusForeground}" />
                                        </Border>
                                    </Grid>

                                    <ProgressBar Minimum="0" Maximum="1" Value="{Binding Progress, Mode=OneWay}"
                                                 Margin="0,16,0,0" Height="6"
                                                 AutomationProperties.Name="Queued download progress" />

                                    <Grid Margin="0,12,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="8" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="8" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="8" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="8" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock Text="{Binding ProgressDetail}" FontSize="10"
                                                       Foreground="{StaticResource MutedTextBrush}" />
                                            <TextBlock Text="{Binding DestinationDirectory}" Margin="0,3,0,0" FontSize="9"
                                                       Foreground="{StaticResource MutedTextBrush}" TextTrimming="CharacterEllipsis" />
                                        </StackPanel>
                                        <Button Grid.Column="1" Style="{StaticResource SecondaryButton}" Content="{Binding ResumeLabel}"
                                                Command="{Binding ResumeCommand}" Padding="12,8"
                                                Visibility="{Binding CanResume, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                        <Button Grid.Column="3" Style="{StaticResource SecondaryButton}" Content="Pause"
                                                Command="{Binding PauseCommand}" Padding="12,8"
                                                Visibility="{Binding CanPause, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                        <Button Grid.Column="5" Style="{StaticResource SecondaryButton}" Content="Cancel"
                                                Command="{Binding CancelCommand}" Padding="12,8"
                                                Visibility="{Binding CanCancel, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                        <Button Grid.Column="7" Style="{StaticResource SecondaryButton}" Content="Open folder"
                                                Command="{Binding RevealCommand}" Padding="12,8" />
                                        <Button Grid.Column="9" Style="{StaticResource SecondaryButton}" Content="Remove"
                                                Command="{Binding RemoveCommand}" Padding="12,8"
                                                Visibility="{Binding CanRemove, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </ScrollViewer>

        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                      IsEnabled="{Binding IsMainContentEnabled}"
                      Visibility="{Binding IsSettingsPage, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid Margin="38,30,38,40" MaxWidth="1020">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="24" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="14" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="14" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="14" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="14" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0">
                    <TextBlock Text="Settings" FontSize="29" FontWeight="SemiBold" />
                    <TextBlock Text="Local defaults. No account, cloud sync, or telemetry."
                               Margin="0,7,0,0" FontSize="14" Foreground="{StaticResource MutedTextBrush}" />
                </StackPanel>

                <Border Grid.Row="2" Style="{StaticResource Card}" BorderBrush="#613143"
                        Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="{Binding ErrorMessage}" TextWrapping="Wrap" Foreground="#FFC4CE" />
                </Border>

                <Border Grid.Row="4" Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="Download defaults" FontSize="17" FontWeight="SemiBold" />
                        <TextBlock Text="Used for new queue items. Existing destinations stay unchanged."
                                   Margin="0,5,0,0" FontSize="11" Foreground="{StaticResource MutedTextBrush}" />

                        <TextBlock Text="DEFAULT DOWNLOAD FOLDER" Style="{StaticResource FieldLabel}" Margin="0,20,0,6" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="10" />
                                <ColumnDefinition Width="90" />
                            </Grid.ColumnDefinitions>
                            <TextBox Text="{Binding DownloadFolder, UpdateSourceTrigger=PropertyChanged}"
                                     AutomationProperties.Name="Default download folder" />
                            <Button Grid.Column="2" Style="{StaticResource SecondaryButton}" Content="Browse"
                                    Click="BrowseButton_OnClick" />
                        </Grid>

                        <TextBlock Text="FILENAME TEMPLATE" Style="{StaticResource FieldLabel}" Margin="0,18,0,6" />
                        <TextBox Text="{Binding FileNameTemplateText, UpdateSourceTrigger=PropertyChanged}"
                                 AutomationProperties.Name="Filename template" />
                        <TextBlock Text="{}Tokens: {title}, {channel}, {videoId}, {index}. Double braces print a literal brace. Default collection names keep their source index."
                                   Margin="0,6,0,0" FontSize="10" TextWrapping="Wrap"
                                   Foreground="{StaticResource MutedTextBrush}" />
                        <CheckBox Content="Include quality in filename (audio bitrate or video resolution)"
                                  IsChecked="{Binding IncludeQualityInFileName}"
                                  Margin="0,12,0,0"
                                  AutomationProperties.Name="Include quality in filename" />

                        <TextBlock Text="DEFAULT QUICK PRESET" Style="{StaticResource FieldLabel}" Margin="0,18,0,6" />
                        <ComboBox ItemsSource="{Binding DefaultDownloadPresets}"
                                  SelectedItem="{Binding DefaultDownloadPreset}"
                                  Width="320" HorizontalAlignment="Left"
                                  AutomationProperties.Name="Default quick download preset" />
                        <CheckBox Content="Show advanced format controls after analysis"
                                  IsChecked="{Binding ShowAdvancedDownloadOptions}"
                                  Margin="0,14,0,0"
                                  AutomationProperties.Name="Show advanced download controls by default" />

                        <TextBlock Text="MAXIMUM CONCURRENT DOWNLOADS" Style="{StaticResource FieldLabel}" Margin="0,18,0,6" />
                        <ComboBox ItemsSource="{Binding MaxConcurrentDownloadOptions}"
                                  SelectedItem="{Binding SelectedMaxConcurrentDownloads}" Width="160"
                                  HorizontalAlignment="Left" AutomationProperties.Name="Default maximum concurrent downloads" />
                        <TextBlock Text="Requests to one provider host are capped at two and pause on server rate-limit guidance."
                                   Margin="0,6,0,0" FontSize="10" TextWrapping="Wrap"
                                   Foreground="{StaticResource MutedTextBrush}" />

                        <CheckBox IsChecked="{Binding EnableAcceleratedTransfers}" Margin="0,20,0,0"
                                  AutomationProperties.Name="Enable accelerated transfers">
                            <StackPanel>
                                <TextBlock Text="Accelerated transfers" FontWeight="SemiBold" />
                                <TextBlock Text="Use up to four validated parallel range workers for large files. Falls back automatically when unsupported."
                                           Margin="0,4,0,0" FontSize="11" TextWrapping="Wrap"
                                           Foreground="{StaticResource MutedTextBrush}" />
                            </StackPanel>
                        </CheckBox>

                        <Border Background="#0E1A2B" BorderBrush="#29405F" BorderThickness="1"
                                CornerRadius="8" Padding="12" Margin="0,20,0,0">
                            <StackPanel>
                                <TextBlock Text="OUTPUT PREFERENCE" Style="{StaticResource FieldLabel}" />
                                <TextBlock Text="MP4 preferred when video quality is equal; higher-quality WebM remains available."
                                           Margin="0,6,0,0" TextWrapping="Wrap" Foreground="#A8B7CE" />
                                <TextBlock Text="Original-quality MP4/WebM/MKV uses FFmpeg stream copy. Optional H.264, H.265, VP9, MP3, AAC, Opus, WAV, and FLAC profiles re-encode locally. No yt-dlp."
                                           Margin="0,4,0,0" FontSize="10" Foreground="{StaticResource MutedTextBrush}" />
                            </StackPanel>
                        </Border>

                        <Grid Margin="0,20,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="160" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding SettingsStatus}" VerticalAlignment="Center"
                                       Foreground="{StaticResource MutedTextBrush}" />
                            <Button Grid.Column="1" Style="{StaticResource PrimaryButton}" Content="Save settings"
                                    Command="{Binding SaveSettingsCommand}" />
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Grid.Row="6" Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="Network" FontSize="17" FontWeight="SemiBold" />
                        <TextBlock Text="One policy is applied to metadata, collections, captions, thumbnails, media, and update requests."
                                   Margin="0,7,0,0" TextWrapping="Wrap" Foreground="{StaticResource MutedTextBrush}" />

                        <TextBlock Text="PROXY MODE" Style="{StaticResource FieldLabel}" Margin="0,18,0,6" />
                        <ComboBox ItemsSource="{Binding ProxyModeOptions}" SelectedItem="{Binding SelectedProxyMode}"
                                  Width="260" HorizontalAlignment="Left"
                                  AutomationProperties.Name="Network proxy mode" />
                        <TextBlock Text="MANUAL PROXY ENDPOINT" Style="{StaticResource FieldLabel}" Margin="0,14,0,6" />
                        <TextBox Text="{Binding ManualProxyUri, UpdateSourceTrigger=PropertyChanged}"
                                 IsEnabled="{Binding UsesManualProxy}"
                                 AutomationProperties.Name="Manual HTTP or HTTPS proxy endpoint" />
                        <TextBlock Text="Example: http://127.0.0.1:8080/. Credentials are rejected and never stored."
                                   Margin="0,5,0,0" FontSize="10" Foreground="{StaticResource MutedTextBrush}" />

                        <Grid Margin="0,18,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="14" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="14" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="METADATA TIMEOUT (SECONDS)" Style="{StaticResource FieldLabel}" />
                                <ComboBox ItemsSource="{Binding MetadataTimeoutOptions}"
                                          SelectedItem="{Binding MetadataTimeoutSeconds}" Margin="0,6,0,0"
                                          AutomationProperties.Name="Metadata request timeout seconds" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="MEDIA RETRY ATTEMPTS" Style="{StaticResource FieldLabel}" />
                                <ComboBox ItemsSource="{Binding DownloadRetryOptions}"
                                          SelectedItem="{Binding DownloadRetryAttempts}" Margin="0,6,0,0"
                                          AutomationProperties.Name="Media retry attempts" />
                            </StackPanel>
                            <StackPanel Grid.Column="4">
                                <TextBlock Text="REQUESTS PER HOST" Style="{StaticResource FieldLabel}" />
                                <ComboBox ItemsSource="{Binding PerHostConcurrencyOptions}"
                                          SelectedItem="{Binding PerHostConcurrency}" Margin="0,6,0,0"
                                          AutomationProperties.Name="Maximum concurrent requests per provider host" />
                            </StackPanel>
                        </Grid>
                        <TextBlock Text="Provider subdomains share this host limit and honor bounded server Retry-After guidance. Changes apply to new requests after Save settings."
                                   Margin="0,10,0,0" FontSize="10" TextWrapping="Wrap"
                                   Foreground="{StaticResource MutedTextBrush}" />
                    </StackPanel>
                </Border>

                <Border Grid.Row="8" Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="Updates" FontSize="17" FontWeight="SemiBold" />
                        <TextBlock Text="Stable releases are downloaded from the official GitHub repository and verified with two matching SHA-256 records before they can run."
                                   Margin="0,7,0,0" TextWrapping="Wrap" Foreground="{StaticResource MutedTextBrush}" />

                        <CheckBox IsChecked="{Binding EnableAutomaticUpdateChecks}" Margin="0,18,0,0"
                                  AutomationProperties.Name="Automatically check for TubeForge updates">
                            <StackPanel>
                                <TextBlock Text="Automatically check at startup" FontWeight="SemiBold" />
                                <TextBlock Text="Checks release metadata only. When an update is found, TubeForge asks before downloading, installing, and restarting."
                                           Margin="0,4,0,0" FontSize="11" TextWrapping="Wrap"
                                           Foreground="{StaticResource MutedTextBrush}" />
                            </StackPanel>
                        </CheckBox>

                        <Grid Margin="0,16,0,0"
                              Visibility="{Binding IsUpdateInProgress, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <TextBlock Text="{Binding UpdateProgressStage}" FontSize="11" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding UpdateProgressPercent}" HorizontalAlignment="Right"
                                       FontSize="11" Foreground="{StaticResource MutedTextBrush}" />
                            <ProgressBar Grid.Row="1" Style="{StaticResource UpdateProgressBar}"
                                         Margin="0,7,0,0" Maximum="1"
                                         Value="{Binding UpdateDownloadFraction, Mode=OneWay}"
                                         AutomationProperties.Name="TubeForge update progress" />
                        </Grid>
                        <TextBlock Text="{Binding UpdateStatus}" Margin="0,14,0,0" TextWrapping="Wrap"
                                   Style="{StaticResource UpdateStatusText}"
                                   AutomationProperties.LiveSetting="Polite" />
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                            <Button Style="{StaticResource SecondaryButton}" Content="Check now"
                                    Command="{Binding CheckForUpdatesCommand}" Padding="14,9" />
                            <Button Style="{StaticResource PrimaryButton}" Content="{Binding UpdateActionLabel}" Margin="10,0,0,0"
                                    Command="{Binding UpdateNowCommand}" Padding="14,9"
                                    AutomationProperties.Name="Download, verify, install, and restart TubeForge"
                                    Visibility="{Binding IsUpdateActionAvailable, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Grid.Row="10" Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="Privacy and responsibility" FontSize="17" FontWeight="SemiBold" />
                        <TextBlock Text="Settings, queue state, and completed-output history stay in local application data. Media URLs, cookies, signatures, visitor data, and telemetry are not persisted."
                                   Margin="0,8,0,0" TextWrapping="Wrap" Foreground="{StaticResource MutedTextBrush}" />
                        <TextBlock Text="Download only media you own or have permission to save. TubeForge does not bypass DRM, payment, membership, or access controls."
                                   Margin="0,7,0,0" TextWrapping="Wrap" Foreground="{StaticResource MutedTextBrush}" />
                    </StackPanel>
                </Border>
            </Grid>
        </ScrollViewer>

        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                      IsEnabled="{Binding IsMainContentEnabled}"
                      Visibility="{Binding IsDiagnosticsPage, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid Margin="38,30,38,40" MaxWidth="1020">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="24" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="14" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="14" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0">
                    <TextBlock Text="Diagnostics" FontSize="29" FontWeight="SemiBold" />
                    <TextBlock Text="Local runtime and extraction health. Sensitive media URLs are never shown."
                               Margin="0,7,0,0" FontSize="14" Foreground="{StaticResource MutedTextBrush}" />
                </StackPanel>

                <Border Grid.Row="2" Style="{StaticResource Card}" BorderBrush="#613143"
                        Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="{Binding ErrorMessage}" TextWrapping="Wrap" Foreground="#FFC4CE" />
                </Border>

                <Border Grid.Row="4" Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="Runtime" FontSize="17" FontWeight="SemiBold" />
                        <Grid Margin="0,16,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="180" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="10" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="10" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="10" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <TextBlock Text="TubeForge version" Foreground="{StaticResource MutedTextBrush}" />
                            <TextBlock Grid.Column="1" Text="{Binding ApplicationVersion}" />
                            <TextBlock Grid.Row="2" Text="Runtime" Foreground="{StaticResource MutedTextBrush}" />
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding RuntimeDescription}" />
                            <TextBlock Grid.Row="4" Text="Architecture" Foreground="{StaticResource MutedTextBrush}" />
                            <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding ProcessArchitecture}" />
                            <TextBlock Grid.Row="6" Text="Dependencies" Foreground="{StaticResource MutedTextBrush}" />
                            <TextBlock Grid.Row="6" Grid.Column="1" Text="{Binding DependencyStatus}"
                                       Foreground="{StaticResource SuccessBrush}" />
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Grid.Row="6" Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="Session and local storage" FontSize="17" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding DiagnosticsExtractionStatus}" Margin="0,14,0,0" />
                        <TextBlock Text="{Binding DiagnosticsFormatSummary}" Margin="0,5,0,0"
                                   Foreground="{StaticResource MutedTextBrush}" />
                        <TextBlock Text="{Binding QueueSummary}" Margin="0,5,0,0"
                                   Foreground="{StaticResource MutedTextBrush}" />

                        <TextBlock Text="QUEUE STATE" Style="{StaticResource FieldLabel}" Margin="0,20,0,5" />
                        <TextBlock Text="{Binding QueueStoragePath}" TextWrapping="Wrap"
                                   Foreground="{StaticResource MutedTextBrush}" />
                        <TextBlock Text="LIBRARY HISTORY" Style="{StaticResource FieldLabel}" Margin="0,14,0,5" />
                        <TextBlock Text="{Binding HistoryStoragePath}" TextWrapping="Wrap"
                                   Foreground="{StaticResource MutedTextBrush}" />
                        <TextBlock Text="SETTINGS" Style="{StaticResource FieldLabel}" Margin="0,14,0,5" />
                        <TextBlock Text="{Binding SettingsStoragePath}" TextWrapping="Wrap"
                                   Foreground="{StaticResource MutedTextBrush}" />
                        <Grid Margin="0,22,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="10" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding DiagnosticsStatus}" VerticalAlignment="Center"
                                       Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" />
                            <Button Grid.Column="1" Style="{StaticResource SecondaryButton}" Content="Copy report"
                                    Click="CopyDiagnosticsButton_OnClick" Padding="14,9"
                                    AutomationProperties.Name="Copy redacted diagnostic report" />
                            <Button Grid.Column="3" Style="{StaticResource PrimaryButton}" Content="Export JSON"
                                    Click="ExportDiagnosticsButton_OnClick" Padding="14,9"
                                    AutomationProperties.Name="Export redacted diagnostic report as JSON" />
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>
        </ScrollViewer>

        <Border Grid.ColumnSpan="2" Background="#E6080C14" Panel.ZIndex="100"
                FocusManager.IsFocusScope="True" KeyboardNavigation.TabNavigation="Cycle"
                AutomationProperties.Name="Responsible use acknowledgement"
                Visibility="{Binding ShowResponsibleUseNotice, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border Style="{StaticResource Card}" Width="560" HorizontalAlignment="Center" VerticalAlignment="Center"
                    Padding="30" BorderBrush="#3A4770">
                <StackPanel>
                    <Border Width="52" Height="52" CornerRadius="15" Background="{StaticResource AccentBrush}"
                            HorizontalAlignment="Left">
                        <Viewbox Margin="9">
                            <Grid Width="32" Height="32">
                                <Path Fill="White" Data="M 8,5.9 C 7.1,5.5 6.3,6 6.3,6.8 L 6.3,25.2 C 6.3,26 7.2,26.5 7.9,26.1 L 24,16.8 C 24.7,16.4 24.7,15.5 24,15.1 Z" />
                                <Path Fill="#FFAD42" Data="M 25,2 L 26,5 L 29,6 L 26,7 L 25,10 L 24,7 L 21,6 L 24,5 Z" />
                            </Grid>
                        </Viewbox>
                    </Border>
                    <TextBlock Text="Use TubeForge responsibly" FontSize="24" FontWeight="SemiBold" Margin="0,22,0,0" />
                    <TextBlock Text="TubeForge saves public media locally without ads, tracking, or hosted conversion. You remain responsible for what you download."
                               Margin="0,10,0,0" TextWrapping="Wrap" LineHeight="20"
                               Foreground="{StaticResource MutedTextBrush}" />
                    <TextBlock Text="• Download only media you own or have permission to save."
                               Margin="0,18,0,0" TextWrapping="Wrap" />
                    <TextBlock Text="• Follow copyright, privacy, local law, and applicable service terms."
                               Margin="0,8,0,0" TextWrapping="Wrap" />
                    <TextBlock Text="• TubeForge does not bypass DRM, payments, membership, or access controls."
                               Margin="0,8,0,0" TextWrapping="Wrap" />

                    <Border Background="#0E1A2B" BorderBrush="#29405F" BorderThickness="1"
                            CornerRadius="8" Padding="12" Margin="0,18,0,0">
                        <StackPanel>
                            <TextBlock Text="QUICK START" Style="{StaticResource FieldLabel}" />
                            <Grid Margin="0,7,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="8" />
                                    <ColumnDefinition Width="76" />
                                </Grid.ColumnDefinitions>
                                <TextBox Text="{Binding DownloadFolder, UpdateSourceTrigger=PropertyChanged}"
                                         AutomationProperties.Name="First run download folder" />
                                <Button Grid.Column="2" Style="{StaticResource SecondaryButton}" Content="Browse"
                                        Click="BrowseButton_OnClick" Padding="9,7" />
                            </Grid>
                            <ComboBox ItemsSource="{Binding DefaultDownloadPresets}"
                                      SelectedItem="{Binding DefaultDownloadPreset}"
                                      Margin="0,9,0,0"
                                      AutomationProperties.Name="First run default download preset" />
                            <CheckBox Content="Check for verified updates at startup"
                                      IsChecked="{Binding EnableAutomaticUpdateChecks}"
                                      Margin="0,9,0,0" />
                            <CheckBox Content="Show advanced format controls"
                                      IsChecked="{Binding ShowAdvancedDownloadOptions}"
                                      Margin="0,7,0,0" />
                        </StackPanel>
                    </Border>
                    <TextBlock Text="Acknowledgement is stored only on this device."
                               Margin="0,18,0,0" FontSize="10" Foreground="{StaticResource MutedTextBrush}" />
                    <TextBlock Text="{Binding ErrorMessage}" Margin="0,12,0,0" TextWrapping="Wrap"
                               Foreground="#FFC4CE"
                               Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    <Button x:Name="ResponsibleUseAcceptButton" Style="{StaticResource PrimaryButton}"
                            Content="I understand — continue" Command="{Binding AcceptResponsibleUseCommand}"
                            AutomationProperties.Name="Accept responsible use and continue" Margin="0,22,0,0" />
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</Window>
